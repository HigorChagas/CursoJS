// OBS: Constantes não podem ser criadas utilizando palavras reservadas.
// Constantes precisam ter nomes significativos.
// Não pode começar o nome de uma constante com números.
// O nome das constantes não podem conter espaços ou traços.
// JavaScript utiliza a notação camelCase.
// As constantes são Case-sensitive (letras maiúsculas e minúsculas fazem diferença).
// Não podemos modificar os valores de uma constante
// NÃO UTILIZA VAR, UTILIZE CONST.
// As constantes devem ser criadas e inicializadas.

const nome = 'Higor';
const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
let resultadoTriplicado = resultado * 3;
resultadoTriplicado = resultadoTriplicado + 5; 

console.log(typeof nome);