// String, Number, undefined, null, boolean, symbol

const nome = 'Higor'; //Strings
const numero = 10; //Number
const numeroDois = 10.52 // Number
let nomeAluno; // Undefined = não aponta para local nenhum na memória.
const sobrenomeAluna = null; // Nulo -> não aponta para local nenhum na memória.
let boolean = true; // Boolean = true, false (lógico)


console.log(typeof boolean);

